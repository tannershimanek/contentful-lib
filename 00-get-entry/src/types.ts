export type CFOptions = {
  spaceId: string;
  environmentId: string;
  token: string;
};

export type CLIConfig = {
  spaceId: string;
  environmentId: string;
  accessToken: string;
  entryIds: string[];
}